@model IEnumerable<Medictionary.Models.User>

@{
    Layout = "_Layout";
}
@if (!Model.Any())
{
    <p>No stockiests found.</p>
}
else
{
    <div class="stockiest-cards">
        @foreach (var stockiest in Model)
        {
            <div class="stockiest-card">
                <div class="stockiest-card-header">
                    <h3>@stockiest.UserName</h3>
                </div>
                <div class="stockiest-card-body">
                    <p><strong>Address:</strong> @stockiest.Address</p>
                    <p><strong>Contact:</strong> @stockiest.PhoneNumber</p>
                    <p><strong>Email:</strong> @stockiest.Email</p>
                </div>
            </div>
        }
    </div>
}

<style>
    .stockiest-cards {
        display: grid;
        grid-template-columns: repeat(2, 1fr); /* Two columns */
        gap: 20px;
        padding: 20px;
    }

    .stockiest-card {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        padding: 15px;
    }

    .stockiest-card-header {
        background-color: #414464;
        color: white;
        padding: 10px;
        text-align: center;
        width: 100%;
    }

    .stockiest-card-body {
        padding: 15px;
        width: 100%;
    }

    .stockiest-card-body p {
        margin: 5px 0;
    }

    @* /* Responsive Design */
    @media (max-width: 768px) {
        .stockiest-cards {
            grid-template-columns: 1fr; /* One column */
        }
    } *@
</style>